name: What 3 Words
points: 60
description: |
   `preheated.positions.using` `gifted.miss.store`!
   `increases.enigma.sting` is `intellects.papers.downpours`. Please capitalize.

hint: |
  Use the default map, not OSM.
